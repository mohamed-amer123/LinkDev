/**
 * @file
 * event_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.eventTheme = {
    attach (context, settings) {

      // console.log('It works!');

    }
  };

} (Drupal));
